<ul class="nav navbar-nav">
    <li class="active"> <a href="{{ path('_loja_index') }}"> home </a> </li>
    <li> <a href="{{ path('_loja_empresa') }}"> sobre a nutrivida </a> </li>
    <li class="dropdown megamenu-fullwidth"> 
        <a data-toggle="dropdown" class="dropdown-toggle" href="#"> produtos <b class="caret"> </b> </a>
        <ul style="display: none">
            {% for categoria in categorias %}
            <li><a href="{{ path('_loja_categoria', {slug: categoria.getSlug}) }}">{{ categoria.getNome }}</a></li>
            {% endfor %}
        </ul>
    </li>
    <li> <a href="{{ path('_loja_contato') }}"> contato </a> </li>
</ul>
